# Lista automática de paths existentes propuesta para archivar

Esta lista fue generada automáticamente a partir del contenido actual de la raíz del workspace.
Se excluyeron carpetas y archivos considerados esenciales: `rebuild/`, `.git/`, `.github/`, `.venv/`,
`.env`, `.env.example`, `requirements.txt`, `.devcontainer/`, `.vscode/` y archivos de cache.

Revisa antes de aplicar. Para aplicar los movimientos con el script existente:

```
cd archive
pwsh .\apply_proposal.ps1 -Apply
```

Paths detectados (relativos a la raíz) y propuestos para mover:
- check_dataverse_token.py
- file.png

Si quieres que aplique el movimiento ahora, responde "aplica la lista".
